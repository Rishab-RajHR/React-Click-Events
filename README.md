Click Events in React
A click event is triggered when a user presses and releases a mouse button on an element.

Click events are mostly handled using addEventListener() or inline attributes like onclick.

Useful for buttons, links, cards, UI elements, or triggering actions like form submission, navigation, or animations.

Common syntax:
